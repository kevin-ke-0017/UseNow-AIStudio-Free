<p align="right"><b>🇨🇳 简体中文</b> · <a href="CHANGELOG.en.md">🇬🇧 English</a></p>

# 更新日志 · Changelog

本页只记录对用户可见的变化。下载最新版：**[Releases](../../releases/latest)**

---

## V10.1 · 2026-09-22

**说清楚：一个兑换码 = 同时 1 台电脑 + 1 台手机**

下载页、压缩包里的说明、软件里「设置 → 授权」那段，原来对同一件事有两种说法：
一处写「同时 1 台电脑 + 1 台手机」，另一处写「电脑最多 3 台、手机最多 3 台」。
**后者把「换绑额度」说成了「同时可用台数」，等于白纸黑字多送两台电脑 + 两台手机。**

以实际售卖的口径为准，四处统一成同一句话：

| | |
|---|---|
| 同时可用 | **1 台电脑 + 1 台手机**（两端分开算，一起用不占换绑次数） |
| 免费换绑 | 每端 **2 次** |
| 不算换绑 | 重装软件、清数据、换网卡 |
| 算一次换绑 | 重装系统 / 手机恢复出厂 / 换设备 |

换绑次数用完了联系卖家，卖家可以手动给你签一张新的激活码。

> 这一版程序功能没有任何改动，只是把话说清楚。

---

## V10.0 · 2026-09-22

**修 v9.9.0 发布页与压缩包里那句说反了的话**

V9.9 把「激活后永久离线可用」这句容易误读的话在软件里改正了，
**但下载页和压缩包里那份 `ReadMe-使用说明.txt` 没跟上** —— 于是 v9.9.0 的
Release 页上，「已改正」的说明和那句错误说法同时挂着。程序本身没有任何问题，
只是买家看到的文字说反了，所以补发这一版。

现在这四处（Release 正文中英、压缩包内 ReadMe 中英）和软件里口径一致：

> 激活只需联网这一下，之后**授权不再联网验证**、不会再问第二次。
> 注意这说的是**授权** —— 对话 / 画图 / 视频都要调用 AI 接口，
> **用 AI 功能仍然需要联网**；不联网时软件能打开，本地知识库、历史、导出都能用。

> 发版闸门新增一条，专门拦「永久离线可用 / offline forever」这类说法
> （和拦「永久免费」那条同一个位置）。**上一次漏掉正是因为它只靠人记得。**

---

## V9.9 · 2026-09-22

**设置面板改名，以及三处「尖括号跑到界面上」的修复**

- **「⚙️ API 设置」改名为「⚙️ 设置」。** 里面早就不只有 API 了 —— 授权、个性化、
  界面语言、通用参数、接口诊断日志都在这一页，旧名字反而让人找不到东西。
  界面里所有指向这一页的提示（新手引导、超时提示、身份验证失败提示等）一并改了。
  顺带修掉一个老问题：这个标题原来没接入多语言，**英文界面下它也是中文**。
- **三段说明文字里的 `<b>` `</b>` `<br>` 不再原样显示成尖括号。**
  出现在「设置 → 通用 → 自动重新排队说明」、「设置 → 授权 → 换绑说明」、
  「设置 → 接口诊断日志」三处。原因是这三段走的是「按纯文字填」的那条多语言通道，
  加粗标签被当成文字转义了出来。不影响功能，但看着像没做完的软件。

> 这三处已加上静态断言（`regress.js`）：以后任何一段带格式标签的文案挂错通道，
> 测试当场变红，不会再靠截图发现。

**视频进度条：1% 曾经被显示成 100%**

对着 Agnes Video 2.5 官方文档重新核对了一遍轮询，修掉三处：

- **进度 `1%` 直接显示成 `100%`。** 文档写明 `progress` 是 **0–100 的整数**，
  而产品原来为了兼容「0–1 小数」的服务端写了「≤1 就乘以 100」——
  服务端老老实实回 `progress: 1`（1%）时，进度条当场拉满。
  用户盯着一个 100% 的进度条等好几分钟，只会觉得软件卡死了。
  现在只有真小数（0 与 1 之间）才按比例读，整数一律按 0–100。
- **只有 `status` 为 `completed` 时才算出片。** 文档明确「以 status 和
  metadata.url 为准」。产品原来扒到一个视频地址就宣布完成，可能拿到中间产物。
  现在服务端明说还在排队 / 生成时会继续等；但也不死等 —— 连续 3 轮（≥15 秒）
  都是「有地址、状态却没更新」就认下来，免得白白丢掉一个其实能播的视频。
- **查询任务撞到速率限制会自动降频**（5 秒 → 10 → 20 → 40 → 60 秒封顶，
  恢复正常立刻回到 5 秒），文档对 `429` 的建议就是「指数退避并降低轮询频率」。
  原来撞了限流还是每 5 秒问一次，既一直撞，又把每分钟的预算吃掉，
  连「提交」那条路一起拖慢 —— 同一个额度，两条路在互相抢。
- 顺带把 20 分钟的轮询上限从「按次数」改成**按时间**算。
  原来写的是「240 次 × 5 秒」，一旦有了退避，次数和时长就不是一回事了。

> 轮询间隔仍是 **5 秒**，没有照抄文档建议的 1–2 秒：那等于每分钟 30–60 个请求，
> 而免费额度限的正是每分钟请求数 —— 那恰好就是 V9.8 修的那一整轮速率限制的成因。

**两处说明写得不准，已改正**

- 「激活后**永久离线可用**」容易被理解成「这软件不联网也能用」。实际是：
  **授权**只验一次，激活后开机不再联网验证；但对话、画图、视频都要调用 AI 接口，
  **用 AI 功能仍然需要联网**。不联网时软件能打开，本地知识库、历史、导出都能用。
  激活页上那句话也一起改了。
- 「2.5 的参考图**只接受公网图片链接**」——**不对**，本机上传实测也能出片。
  现在说清两条路：公网链接是官方文档写明的做法、也最稳（画廊里的图自带链接）；
  本机图片按内嵌数据送出，文档没写这条，可能哪天被服务商收紧。
  失败提示也不再断言「这条路走不通」，只说这一次走的是哪条、以及最稳的是哪条。

---

## V9.8 · 2026-09-22

**视频排队：不再被自己的重试掐断，而且「队列满」不再长得像故障**

先说清原来发生了什么，因为它看起来完全像软件有 BUG：

1. 提交视频 → 服务端回 **`503 视频队列已满`**（后端一次只处理 1 个任务，高峰期常见）
2. 产品每 8 秒自动重排 → 25 秒内打出 3 个请求
3. 第 3 个请求撞上**速率限制**（免费额度按每分钟请求数算）
4. 产品把速率限制当成**最终失败** → 一屏红字

**所以「最多排队 20 分钟」这个设计从来没生效过** —— 真实上限是「你的配额允许探几次」，
也就是 3 次、25 秒。那屏红字不是排队失败，是产品自己把排队掐断了。

这一版：

- **排队途中撞到速率限制不再终止排队** —— 它的意思是「别催」，不是「排不上」。
  20 分钟的排队预算第一次真正生效，成功率从靠运气变成靠耐心。
- **新增设置项：⚙️ 设置 →「视频队列满时，自动重新排队」**
  默认开启、间隔默认 **30 秒**（下限 15 秒）、最长等待默认 **20 分钟**，都可改。
  为什么做成可调：安全的间隔取决于**你的 API 额度档位**，而软件看不到它 ——
  免费档和付费套餐差很多，写死一个数就是替你猜。
  **关掉它**的话，队列满了会直接告诉你，由你自己点「🔄 重新排队」。
- **排队中的界面改成中性的「正在自动重新排队 · 已等 3 分 20 秒 · 第 5 次」**，
  不再是红色失败样式 —— 排队不是错误，它只是还没轮到。
- **去掉了撞限流之后那个 60 秒强制等待。**
- **失败提示从默认展开 304 字缩到 121 字**，排查信息（第一次的错误原话、原始响应、
  处置步骤）全部收进「查看原始响应」折叠里 —— 需要的人点开，不需要的人看三行就够。

> ⚠ **最重要的一条使用建议**：**别自己连续快速点「生成视频」。**
> 每次重排都是一个 API 请求，短时间内多次提交很容易触发服务端的速率限制，
> 反而更难排上。交给自动重排，或者把间隔调大。

---

## V9.7 · 2026-09-22

**说清「一个兑换码能激活几台」**

- 激活页底部原来只写「每个兑换码可免费换绑 2 次」，没说电脑和手机是**分开算的**。
  现在写清楚了：

  | | 首次激活 | 免费换绑 | 合计 |
  |---|---|---|---|
  | 💻 电脑 | 1 台 | 2 次 | **3 台** |
  | 📱 手机 | 1 台 | 2 次 | **3 台** |

  **同一个码可以电脑和手机一起用，不占任何换绑次数。**
  在电脑上换满 3 台之后，手机那 3 台依然是满的。
- 也写清了什么**不算**换绑：重装软件、清数据、换网卡都不算（同一台设备再来会把
  原来那张原样重发）；只有重装系统 / 手机恢复出厂 / 真换设备才消耗一次。
- 📄 授权说明里那句「个人使用永久免费」已改正 —— 本产品从 V8.7 起是**付费授权**的。
  买了之后用它接活赚的钱仍然全是你的，不额外分成。

---

## V9.6 · 2026-09-21

**视频提交失败时，现在会告诉你「第一次」是什么错**

- 视频提交遇到「服务繁忙」会自动重试。问题是：重试本身容易撞上速率限制，
  于是**最后你看到的是「速率限制」，而真正让它重试的那个错误一个字都没留下**。
  排查会卡死在这里 —— 看到的永远是重试的后果，不是原因。
- 现在失败提示里会多一段：**这一次实际发了几个请求**，以及**第一次的错误原话**。
  诊断日志里也多了 `busyErr` / `busyStatus` 两个字段。
- 如果第一次那句话看着不像「服务器忙」，那它就是真正的失败原因 ——
  请把它发给开发者。

---

## V9.5 · 2026-09-21

**修复：视频生成其实只等 10 分钟就判超时（本该 20 分钟）**

- 轮询次数被**重复计了两次**，而判超时看的就是这个数。于是代码里写着
  「240 轮 × 5 秒 = 20 分钟」，**实际在第 120 轮、也就是 10 分钟就放弃了**。
  一个本来 12 分钟能出片的任务会被提前丢掉，界面显示「⏱ 超时」——
  而你只会觉得「又失败了」。现在真的是 20 分钟。
- **等待期间的请求量砍掉一半。** 查询进度时会先问新接口，拿不到视频地址就
  再问一遍老接口作为退路 —— 但任务正常在跑的时候本来就还没有地址，
  于是每 5 秒都要白问一次。现在只有新接口真的答不上来才会去问老接口。
  这也顺带降低了触发速率限制的概率。
- 诊断日志里视频那条的 `attempts` 以前是真实轮数的两倍，现在是真实值。

> 这两个都是用户对着诊断日志里的 `attempts` 数字提问才发现的 ——
> 那个数在数学上不可能（125 轮 × 5 秒需要 10 分 25 秒，而实际只过了 6 分 11 秒）。

---

## V9.4 · 2026-09-21

**修复：视频「排队重试」会把「服务繁忙」熬成「速率限制」**

- 视频提交遇到「服务繁忙」时，产品原来是**固定每 8 秒重试一次**。
  一次点击就在 25 秒内打出 3 个请求 —— 而免费额度限的正是「每分钟请求数」。
  结果是：第一个请求只是「繁忙」，到第三个就变成了 429 速率限制。
  **失败是产品自己制造的。**
- 现在改成**递增等待**：8 秒 → 16 秒 → 32 秒 → 最多 60 秒。
  同样重试 3 次，请求密度降到三分之一。
- **撞到速率限制后进入 60 秒冷却**：这段时间点「生成」会被拦住，并告诉你还剩几秒。
  以前 429 之后按钮立刻可按、界面也不说「现在按也没用」，越点限得越死。
  冷却只在内存里，关掉产品重开就会重新试一次。

> 怎么确认是不是这个问题：⚙️ 设置 →「🩺 接口诊断日志」，看视频那几条的 `tries`。
> `tries` 大于 1 就说明那一次点击发了多个请求。

---

## V9.3 · 2026-09-21

**修复：视频生成失败时，把「速率限制」说成了「服务器繁忙」**

- 视频生成失败、服务端回的是「你的 API 账号触发速率限制」（HTTP 429）时，
  产品显示的却是「⏳ 视频后端同时只能处理 1 个任务，等 1–2 分钟再试」。
- **这两件事的处置方向相反**：繁忙是等一等就好，速率限制**等下去不会好** ——
  得放慢频率、或者去 API 服务商那边升套餐。看到「等 1–2 分钟」的人会一直等，
  而那个错误怎么等都不会消失。
- 现在会明确告诉你这是速率限制、不是服务器拥挤，并且给出可执行的三步。
  服务端的原话照旧完整显示，方便你直接贴给服务商。
- 顺带提醒一句：图片页「一次生成几张」是**几张就发几个请求**，
  所以出视频前后密集出图很容易把每分钟额度用掉。提示里也写了这条。
- 图片和对话一直是对的，只有视频这条路漏了。

---

## V9.2 · 2026-09-21

**视频请求现在也进诊断日志了**

- 「🩺 接口诊断日志」原来**只记图片**，而说明上写的是「图片 / 视频」。
  偏偏视频是最慢、最容易卡住的那一条 —— 视频一直不出，你把日志发给客服，
  里面一条视频记录都没有。
- 现在一次视频生成记两条，**分开记**：
  「提交排队了多久、重试了几次」和「任务本身跑了多久」。
  卡在这两段是两回事：前者是视频服务在忙（等就行），后者是任务真的出了问题。
- 任务失败、以及「服务端说完成了却没给视频地址」这种情况也会记下来 ——
  后者界面上显示的是「完成」，你手里却什么都没有，原来事后完全查不到。
- 你自己点「取消」不算失败，不会记。
- 不收集任何新东西：日志里依旧**没有 API Key，也没有提示词内容**（只记长度）。

---

## V9.1 · 2026-09-21

**诊断日志里多了一行「机器码来源」**

- 电脑版的机器码有两条来路：正常情况读的是系统里那个**重装软件、换网卡都不会变**
  的标识；万一读不到，会退而用「计算机名 + CPU + 内存」拼一个凑合的。
  凑合的那个**改个计算机名、加一条内存就会变** —— 机器码一变就要重新激活。
- 以前从外面完全看不出走的是哪条路，只能等到「我怎么又要激活一次」才发现。
  现在 ⚙️ 设置 →「🩺 接口诊断日志」复制出来的开头那两行会写清楚（在「版本」那一行），
  找客服时一眼可辨。
- 只是多一行字，不收集任何新信息：这份日志依旧**不含 API Key，也不含提示词内容**。

---

## V9.0 · 2026-09-21

**修复：设置里看不到机器码**

- **⚙️ 设置 →「🔑 授权」原来显示的是一个占位符「…」**，看不到机器码、复制按钮
  点了没反应、授权状态也是空的。换电脑或重装系统想找机器码时，只能先把授权
  删掉让激活页重新弹出来 —— 等于没有这个入口。现在正常显示了。
- 原因：设置是弹窗，而刷新那段授权信息的代码挂在了一个**用户点不到的入口**上，
  于是它在真实产品里从来没被执行过。

---

## V8.9 · 2026-09-21

**海外用户现在也进得来了**

- **首次打开自动按系统语言选中文或英文**。以前固定中文，而语言开关在产品里面 ——
  海外用户开机看到一整页中文的协议和激活页，连一个能点的开关都找不到。
- **版权页和激活页的右上角都加了 🌐 语言开关**，进产品之前就能切。
  自动判断错了也有救。你自己切过语言之后，系统语言再也不会覆盖你的选择。
- 版权页开场那段说明以前是写死的中文，切语言不跟着变，现在中英双语。
- 激活失败的提示**不会再被切语言弄丢**（它会用新语言重新显示一遍）——
  那个括号里的编号是判断问题的唯一依据，丢了就等于线索断了。

---

## V8.8 · 2026-09-20

**新的默认文本模型 · 视频多了 21:9**

- **对话默认换成 `agnes-3.0-flash`**（免费，最新一代，同样能发图识图）。
  上一代 `agnes-2.5-flash` 仍在 ⚙️ 设置的下拉里，随时可以切回去。
- **视频画幅新增「超宽 21:9」**（1680×720）。这一档只有 `agnes-video-2.5-flash`
  有，切到 `agnes-video-v2.0` 时会自动收起并拨回 16:9。
- **`agnes-video-2.5-flash` 标注为「限时免费」**，和官方口径一致。
- **「🎞️ 多图 / 音频参考」的说明按模型分开写**：2.5 这一代走 reference 模式，
  参考图**最多 5 张**（只传 1 张也成立）、参考音频最多 3 段；V2.0 是关键帧补间，
  仍然只收 2–3 张。以前不分代，都写成「2–3 张关键画面」。
- **「怎么一次选多张图」的提示改成按设备说**：电脑上是「按住 Ctrl 再点」，
  手机上是「在系统相册里长按再点其他的」。原来固定写着
  「按住 Ctrl（Mac 是 ⌘）」—— 手机上根本没有这两个键，而本产品也没有 Mac 版。

---

## V8.7 · 2026-09-12

**重要：本版起需要激活码**

有人在二手平台转卖本产品，所以本版加入了按设备绑定的激活授权。

- **首次打开会显示本机机器码**（形如 `W-7K2M-4XQ8-VD3N-PB9F`，`W`=电脑、`A`=手机）。
  两种激活方式：填卖家给的**兑换码**一键自动激活；或者把机器码发给卖家、
  把他给的**激活码**粘进来（没网络时用这个）。
- **激活后永久离线可用**，不会再问第二次 —— 只有激活那一下需要联网。
- **机器码不含任何硬件信息**，也不含你的 API Key 和提示词，只是设备标识的哈希，
  可以放心发给卖家。随时能在 ⚙️ 设置 → 授权 里查看和复制。
- **重装软件、换网卡、插 U 盘都不会让授权失效。** 重装系统 / 手机恢复出厂 / 换设备会，
  需要新的激活码；每个兑换码**免费换绑 2 次**，电脑和手机分开计数
  （两端都用**不占**换绑次数）。
- 激活失败的提示里带**括号编号**，把它和机器码一起发给卖家就能判断：
  `E1` 码没复制全 · `E2` 码无效 · `E3` 这个码是发给别的设备的 · `E4` 已到期 ·
  `E5` 本机无法校验（请使用官方渠道的安装包）。

**老用户升级请按这个顺序来**（本版不会自动继承旧版的授权）：

1. 在**旧版**里打开【设置】→【💾 数据备份与恢复】，点「📤 导出全部数据」存下那个 .zip
2. 卸载 / 删掉旧版
3. 安装新版，用激活码激活
4. 在新版同一个地方点「📥 导入并恢复」，选第 1 步那个 .zip

顺序别颠倒 —— 先删了旧版再想起导数据，历史记录和收藏就找不回来了。

## V8.6 · 2026-09-05

**改动**
- **画廊里的「🎬 视频」按钮现在一键喂两种玩法**：既设为「图生视频」的首帧，
  也加进「多图 / 音频参考」的参考图列表。之前只进前者，想用多图还得再传一次
  —— 而 2.5 只收公网链接，重传本身就走不通。已在列表里的图不会重复添加，
  超过当前模型的张数上限时不再挤掉已有的。
- **参考图与所选画幅不一致时会直接提示。** 成片按你选的画幅出，参考图比例对不上
  就会被裁切或拉伸；现在参数区会写清楚「参考图 16:9 · 所选 9:16」，匹配时也给个确认。
  图生视频、多图参考、以及 V2.0 的关键帧都覆盖到。
- **三个模型下拉去掉「自定义…」**（文本 / 图片 / 视频）。官方模型固定且免费，
  自定义项只是让人误填出错。**这条取代 V8.5 里「选自定义…手填」的说明。**
  以前手填过别家模型的用户不受影响：保存的值会作为「（当前设置）」出现在下拉里，
  照旧可用，不会被悄悄改掉。

**修复**
- 修掉一处自己引入的回归：切到 2.5 模型后，参考图画幅提示整段没有显示。
- 修掉一处提示文案 bug：同一个占位符在一句话里出现两次时，只有第一处被替换，
  第二处会把 `%a` 之类的符号原样显示给用户。全库扫了一遍，图片速率提示里
  还有同一个毛病，一并修掉，并加了一条静态检查钉住这一类。

## V8.5 · 2026-09-05

**新增 / 改动**
- **对话默认模型换成 `agnes-2.5-flash`（免费）。** 官方已宣布 `agnes-2.0-flash` **废弃**，
  2.5 Flash 在编码、工具调用、多轮一致性和图像理解上全面升级，接入方式完全兼容。
- **三个模型栏都改成下拉，并标注「（免费）」**：文本模型、图片模型、视频模型。
  官方模型固定且免费，所以**移除了「获取模型」按钮** —— 它要求先填对 Key 才能用，
  对新用户是个多余的门槛。需要用别家模型的，在下拉里选「自定义…」照旧手填。
- **视频模型下拉直接提供两个选项**：`agnes-video-2.5-flash` 与 `agnes-video-v2.0`，
  在设置页和视频生成页两处都能切，两边互相同步。
- **使用指南同步更新**（中英两份）：三种视频玩法、页面内切模型、两代模型的差异、
  参考音频、接口诊断日志、图片生成超时可调、反推提示词，全部写进去了。

**修复**
- 修掉一个初始化时的内部错误（视频模型相关常量在被读取之后才定义）。
  它不会让界面报错，只会让初始化中途静默中断 —— 属于最难发现的一类。

## V8.4 · 2026-09-05

**新增**
- **默认换用 Agnes Image 2.5 Flash**（官方最新一代，当前免费）。它的整体能力全面超过
  2.1 Flash，接入方式、尺寸档位和价格完全一致，所以只是换个模型名，用法不变。
  设置里的图片模型现在带下拉，两代随时切换。**已保存过设置的老用户不受影响**，
  想升级就在 ⚙️ 设置 → 图片模型 里选 `agnes-image-2.5-flash`。
- **视频页新增模型快速切换**。不用再进设置——在视频生成页顶部直接选，
  选完界面立刻按该模型的规则重排（清晰度、时长范围、帧率、参考图张数、音频入口）。
- **支持 Agnes Video 2.5 的参考音频**（最多 3 段）。切到 2.5 且在「多图 / 音频参考」
  模式下会出现音频上传入口；提示词里可以用 `<Audio 1>` 指代第 1 段。
- **手机上的菜单按钮加了「菜单」两个字**，并在首次打开时轻微闪动一次。
  有用户反馈只看到 ☰ 图标，**以为这个 App 只有对话一个页面**。

**改动**
- **「关键帧动画」并入「图生视频（首/尾帧）」。** 这两个入口送出的是**完全相同的请求**
  ——两代都是——区别只在于给一张还是两张图。合并后：只传首帧就是图生视频，
  再传一张尾帧就是首尾帧过渡。四个标签变三个。
- 多图模式的张数提示现在跟着模型走：Agnes Video 2.5 最多 **5 张**，V2.0 仍是 2–3 张。
  此前切到 2.5 之后提示仍写着「2–3 张」。

## V8.3 · 2026-09-05

**新增**
- **支持 Agnes Video 2.5 / 2.5 Flash**（服务商当前限时免费）。在 ⚙️ 设置 → 视频模型
  填 `agnes-video-2.5-flash` 即可切换，界面会自动按这一代的规则调整：
  清晰度锁定 720P、时长限定 4–12 秒、帧率选项隐藏（它没有这个参数），
  规格行按官方尺寸表显示真实输出（如 9:16 → 720×1280）。
  > **注意模型 ID 没有字母 v**：新一代是 `agnes-video-2.5-flash`，
  > 只有旧的 V2.0 才是 `agnes-video-v2.0`。填错会收到「无可用渠道」，
  > 现在遇到这个错会直接提示你拼写问题。

**已知限制（来自服务商，不是本产品的取舍）**
- **2.5 的参考图只接受公网可访问的图片链接。** 从本机选的图片没有网址，
  这条路是否可行未经证实。可行的做法：先在「图片生成」里出图（画廊里的图自带链接），
  再点「参考图」选它；或把视频模型切回 `agnes-video-v2.0`（V2.0 支持直接上传本地图片）。
  遇到相关报错时界面会直接说明这一点。
- **2.5 不支持负面提示词**（官方参数表里没有这一项），所以在该模型下不再发送 ——
  发了也只会被静默忽略，反而让人误以为生效了。

**其它**
- 视频模型切换后，两代的请求格式完全隔离：`agnes-video-v2.0` 的行为一个字节都没变。

## V8.2 · 2026-08-26

**修复**
- **图片生成偶尔一直停在「生成中」，不出图也不报错。** 服务端繁忙时会出现「接了请求
  但迟迟不回包」的情况，而图片请求此前**没有任何超时**——界面就一直转下去，按钮不复位，
  什么提示都没有。现在最多等 3 分钟，超时会明确告诉你「服务端没回包」，并给出一行
  可以直接转发给 API 服务商的信息（时间、线路、模型、症状）。
- **出错时看不到 HTTP 状态码。** 此前报错只有一句话，没有「500」这类状态码，
  你想反馈给服务商都说不清。现在所有失败都带上状态码，例如「HTTP 500：…」；
  服务端返回网页而不是数据时（网关故障常见），也不再显示看不懂的
  「Unexpected token '<'」，而是直接说明是服务端错误。
- **一次生成多张时，只要有一张卡住，已经成好的也会一起丢掉。** 现在成好的照常入库，
  失败的单独说明，例如「只成功 3 张，1 张失败：请求超时」。

- **新增「🩺 接口诊断日志」（⚙️ 设置里）。** 记录最近 100 次图片 / 视频请求的耗时与结果。
  生成很慢或一直不出图时，点「复制诊断日志」，把它发给 API 服务商或开发者，
  就能看出是等在服务端、还是本机出了问题 —— 不用再靠猜。
  日志只记时间、线路域名、模型、耗时和 HTTP 状态码，**不含 API Key，也不含提示词内容**。

- **新增「图片生成超时（秒）」设置项**（⚙️ 设置 → 通用，默认 180 秒，可调 30–900）。
  服务商拥堵时排队几分钟才出图是可能的，写死 3 分钟就等于「一堵就永远生成不了」。
  怀疑只是排队慢，把它调大再试即可。（注意：上面那项「超时（秒）」只管对话，两者互不影响。）

> 这三条都属于服务端出状况时的表现。**产品这边不能替服务商解决故障，但至少要让你
> 知道发生了什么、并且能把该说的话说清楚。**

## V8.1 · 2026-08-24

**修复**
- **电脑版点「➕ 新建文件夹」没有任何反应。** 客户报上来的。原因是桌面版的运行时
  （Electron）**不支持** 浏览器那个输入框弹窗，调用它不是返回空，而是直接抛异常，
  于是点击回调后面的代码一行都不跑 —— 不弹窗、不报错、什么都没有。
  手机版和浏览器都支持，所以只有装了 exe 的人会撞上。
  > 同一个原因影响的不止知识库那一个按钮：**图片收藏夹、视频收藏夹、历史收藏夹、
  > 知识库子文件夹，一共 5 个「新建文件夹」入口在电脑版上全是失效的。**
  > 现在统一换成了应用内的输入弹窗，五处一起修好；回车确定、Esc 取消、
  > 点空白处取消，手机上按返回键也能关。
- **手机版知识库里，文件夹和文档的名字全部看不见。** 那一行里只有名字可以被压缩，
  其余（折叠箭头、图标、条目数、一排按钮）都是固定宽度，屏幕一窄名字就被挤成 0 宽。
  现在窄屏下按钮自动换到第二行，名字拿回完整宽度。图片/视频/历史的收藏夹目录
  是同样的结构，一并修好。
- **「复制」按钮偶尔点了没反应。** 对话消息和代码块这两个复制按钮少写了失败兜底，
  剪贴板一旦被系统拒绝（窗口没聚焦、权限没给）就静默失败。现在全站的复制都走同一个
  入口，失败自动退回旧方式，并且一定会给提示。

**其它**
- 电脑版做了一次全站排查：在真实的桌面运行时里把七个页面的按钮逐个点了一遍，
  确认没有第二处「点了没反应」。这三类问题现在都有自动检查守着，改回去会直接报错。

## V8.0 · 2026-08-20

**修复**
- **「多图视频」一直生成不了，一提交就报 HTTP 400。** 服务端的原话是
  `This request contains multiple images, but mode was omitted`——送多张图时
  必须同时声明 `mode=keyframes`，我们漏了这个字段，于是这条工作流从加进来那天起
  就没有成功过。现在补上了。
  > 顺带纠正一个更根本的误解：Agnes 只有三种形态——不带图（文生视频）、
  > **恰好 1 张**（图生视频）、**2–3 张且 mode=keyframes**（关键帧）。
  > 并不存在「不带 mode 的多图模式」。所以多图视频的张数上限从 8 张改成
  > **服务端真正允许的 3 张**，选到第 4 张时界面会直接标红，而不是等到提交才报错。
- **多图视频没法一张一张地加**，每次选择都会把上一批整个顶掉，只能按住 Ctrl 一次选完。
  和上一版图片参考图同一个原因，现在改成同样的累加清单：每次选择都追加，
  缩略图带序号、可单独删除，界面上也写明了怎么继续加。
- **切到 English 后，参考图下面那行操作说明仍是中文。** 那行字是 JS 拼出来的、
  不带翻译标记，切语言时扫不到。同类的几处一并修好并加了回归断言。

**其它**
- 产品页的界面预览截图全部重拍（中英各六张），此前停在 V7.0 —— 图片生成那张
  还没有「🔍 反推提示词」，视频生成那张也没有负面提示词与随机种子。

## V7.9 · 2026-08-16

**修复**
- **「国内直连」线路的地址是错的，用中国站注册的 Key 一律提示「令牌无效」。**
  原先填的是 `apihub.agnes-ai.cn`，而中国站官方文档给的是 `api.agnes-ai.cn`。
  错在这里特别难查：那个域名是活的、会正常响应，只是不认中国站控制台新建的 Key，
  于是报错看起来像「Key 填错了」，怎么重贴都没用。现在点「国内直连」填的是正确地址。
  > 已经保存过设置的老用户不受影响，配置不会被覆盖；如果你之前也卡在「令牌无效」，
  > 进 ⚙️ API 设置点一下「国内直连」按钮即可更新。

- **图片尺寸菜单里 16 个选项，只有 3 个是模型真正支持的尺寸。** 其余 13 个（如
  16:9 的 2560×1440、4K 的 3840×2160）都不是原生输出尺寸，服务端会**不报错地**
  把它们映射到最接近的档位——你选的和你拿到的不是一回事，而且从界面上看不出来。
  现在菜单按官方的原生尺寸表重建（1K / 2K / 3K / 4K × 五种画幅，共 20 项），
  标签上写的就是实际出图尺寸。
  > 顺带去掉了 7:4 —— 这个画幅模型本来就不支持；新增了 3K 档。

**新增**
- **多图合成。** 参考图从只能放 1 张改成一份清单：1 张是图生图，**2 张以上就是
  多图合成**——把几张图里的元素组合成一张新图，比如「把这个人物和那个产品放进
  同一张海报」。
  - **一张一张地加就行**，每次选择都追加到清单里，不会把上一张顶掉；
    想一次选多张，按住 Ctrl（Mac 是 ⌘）再点。界面上就写着这句话。
  - **画廊里每张图的「🖼️ 参考图」按钮也是点一次加一张**，可以和本地上传的图
    混在一起用——以前这两条路互相顶掉，只能二选一。
  - 每张缩略图右上角都有 ✕ 单独删掉，缩略图上带序号。
  - 张数不设上限：**Agnes 的文档没写多图合成能放几张**，所以不替它编一个数字。
  - 「🪄 AI 优化提示词」也认得多图，会按「每张图的角色 + 目标场景 + 图之间的关系」来写。
- **🔍 反推提示词。** 选一张参考图，点一下就能得到一段能复现它的英文提示词，
  可**一键复制**或**一键填进提示词框**。看到喜欢的图，不用再自己憋着描述。
  > 这个功能用的是你设置里的**文本模型**（不是图片模型），需要它支持读图。
  > 如果模型不支持，会直接告诉你换一个多模态模型，而不是甩一句看不懂的报错。
- 选了 3K / 4K 且数量大于 1 时，会提前提醒：**这两档服务商每分钟只放行 1 张**，
  一次要 4 张必然有几张失败。免得你以为是软件坏了。

**移除**
- **图片的「负面提示词」输入框已删除。** 中国站图片文档的参数表里根本没有这一项——
  填了不会报错，只是被当作不存在，等于一个装饰。留着比没有更糟：你以为它在起作用。
  > 视频那边的负面提示词是文档写明支持的，**保留不动**。

**改进**
- 测试连接失败且提示与令牌 / Key 有关时，线路那一行会直接告诉你该换成哪个地址，
  而不是只把服务端那句原话弹出来——**这类报错十次有九次是线路选错，不是 Key 打错。**
- 聊天里用「/画 一只猫 16:9 4K」这类指令时，也一起走上面的原生尺寸，并支持
  2:3、3:2、21:9 三种新画幅。

## V7.8 · 2026-08-13

**修复（电脑版与安卓版）**
- **图生视频的「尾帧图片」一直没有生效。** 上传后能看到缩略图、视频也能正常生成，
  但那张尾帧根本没送到模型手里——生成结果与只传首帧完全一样。现在首尾帧会按
  官方的「首尾过渡」方式提交，尾帧真正参与生成。
- **关键帧动画只上传一张图也能提交。** 关键帧动画是在两张画面之间补间，
  只有一张时要么报错、要么退化成图生视频。现在会明确提示需要首帧和尾帧两张。

**新增**
- **视频支持负面提示词。** 填写不希望出现的内容（如 blurry、watermark），
  和图片生成的用法一致；留空则不生效。
- **视频支持随机种子。** 填同一个数字可以复现同一次结果，方便微调提示词时
  只改文字、不换画面；留空则每次都不一样。

## V7.7 · 2026-08-12

**新增**
- **安卓版**。与桌面版共用同一份程序，界面、数据格式、API 设置完全一致。
  APK 直接安装，无需应用商店。语音输入与朗读在手机端不提供（系统 WebView 不支持）。

**修复（手机端）**
- **保存图片/视频会莫名跳到浏览器。** 点页面上的下载链接时，系统把它当成普通网址
  交给了外部浏览器，于是要先跳一次浏览器、等图片加载完才能存。现在点下载直接存进
  **系统相册**（其它文件存进「下载」文件夹），存完给一句提示，不再绕。
- **顶栏紧贴状态栏。** 按钮按不准、一不小心就下拉了通知栏；某些面板打开后顶部
  与状态栏重叠，连关闭按钮都点不到。现在全部让开状态栏与手势条。
- **顶栏往右滑之后，左上角的菜单按钮跟着滑走了**，要滑回去才能开菜单。现在钉在原位。
- **看不到版本号。** 手机上底部信息栏是隐藏的（会挡住输入框），版本号也跟着不见了。
  现在显示在侧边菜单底部。
- **使用指南里仍写着手机上没有的语音功能。**
- 返回键/返回手势现在能逐级关闭：侧边菜单 → 弹窗 → 回到对话页 → 连按两次退出。
  之前打开技能/历史面板时按返回会直接退出程序。
- 手机上的按钮、弹窗尺寸按触控习惯放大。

## V7.6 · 2026-08-12

**修复**
- **「已等待」显示的是假时间。** 它取的是「轮询次数 × 5 秒」，而 `genVideo` 从不清理
  上一条轮询链 —— 每提交一次就多一条链在跑，几条链各算各的次数、又往同一块界面写，
  于是出现「已等待约 5.9 分钟 · 3:27」这种自相矛盾的显示。现在只用真实墙上时间，
  一次生成只保留一条轮询链（旧链带序号自动退场）。
- 长时间等待的提醒触发得太早。阈值改为按**这一档的实测典型耗时**的 2.5 倍来判断，
  而不是拍一个固定分钟数；提示里也直接写明「这一档平时约 N 分钟出片」。

**新增**
- **进度条回来了，而且这次有依据。** 服务端给了真进度就用真的；没给就按这一档的
  实测典型耗时估算，并明确标注「预计」。一旦超过典型耗时的 1.5 倍就**停止估算**、
  退回不确定态 —— 可以估，但不能假装是实测。默认值来自实测（480p≈100 秒、
  720p/1080p≈3 分钟），每成功一次就记一笔，取最近 8 次的中位数，越用越准。
- 参数区结果行新增「**约 N 分钟出片**」，点生成之前就知道大概要等多久。
- **视频记录显示完整参数**：生成模式（文生/图生/关键帧/多图）、分辨率、清晰度档位、
  画幅、时长、帧率、开始→结束时间；图生视频还会显示**当时用的参考图缩略图**。
  老记录没有这些字段，就一条都不显示，不编。
- **本地上传的参考图补上移除按钮（✕）**。此前只有「从画廊选取」的图有，自己传的
  没有，只能靠重新选一张覆盖。图生图、视频首帧/尾帧/多图四处一起补齐；
  多图模式下可以单独移除其中一张。

## V7.5 · 2026-08-11

**撤回上一版的判断**

实测发现：**正常的参考图同样会零进度排队两分多钟，最后成功出片。** 也就是说
「长时间零进度 = 卡住」这个前提是错的 —— 服务端对正常排队的任务同样一路报
progress 0。V7.2/V7.4 据此下的「⚠️ 疑似卡住·几乎可以确定不是慢」是站不住的断言，
会劝你放弃本来会成功的任务，比原来的问题更糟。已全部撤回：

- 删除「⚠️ 疑似卡住」这个状态与全部相关断言。
- 长时间等待的提示改为**中立陈述**，并推迟到 5 分钟：明说正常排队也可能这么久、
  继续等仍有可能成功；同时说明另一种可能是卡在内容审核，而**这两种从外面看一样，
  本页分辨不了**。继续等还是停止，交给你决定。
- 删除「参考图前科记忆」。它把「超时」当成「这张图有问题」的证据，而这个证据
  现在被证明不可靠 —— 正常图排队超时同样会被记上一笔，反而误导。
- 「停止等待」和「超时」两屏不再显示 `video_id` 和「再次查询」。实测再查也拿不到
  结果，对用户既看不懂又没用，只是多点一次、多失望一次。现在只说清参考图和参数
  都还在，可以直接重试。

> 另外验证了一件事：把同一张图发给对话接口，它会正常描述，不会拒绝。
> 说明图片的内容审核只发生在视频生成流水线内部，客户端**无法**提前探知 ——
> 「提交前就告诉你能不能过」这件事做不到，与其做个半吊子的猜测，不如老实说不知道。

## V7.4 · 2026-08-11

**新增**
- **参考图会被记住。** 实测确认：同一张图以内嵌方式送出时，服务端既不拒绝也不开始，
  只是一直挂着直到超时 —— 也就是说「一开始就告诉你被拒了」这件事，服务端根本不配合。
  那就由本地来记：一张参考图如果**被明确拒绝过**，或者**提交后一直没能开始处理、
  最后超时**，下次一选中它，参数下方立刻直说，不必再花几分钟去撞同一堵墙。
  只提醒，不阻止你继续。

**改进**
- 进度条的不确定态换成一束平滑扫过的光带，去掉原来的黑色条纹 ——
  条纹既不好看，也容易被误看成「已经完成了几格」。
- 止损时间收紧：**2 分钟**给出可能原因和「停止等待」，**5 分钟**仍然零进度就标为
  「⚠️ 疑似卡住」（原为 3 / 8 分钟）。实测三四分钟就足以判断，没必要再多等。

## V7.3 · 2026-08-11

**改进**
- 排查区新增一行「参考图送法」。同一张图从**本地上传**和从**画廊选取**，
  送给服务端的东西可能不是一回事：画廊里的图如果是一条链接、而本机又取不回来，
  就会把链接原样送上去；其余情况一律先取回本地、裁好再内嵌上传。
  服务端对「链接」和「内嵌图片」的内容审核未必走同一条流水线 ——
  这可以解释为什么同一张图有时立刻被拒、有时挂到超时。
  现在出错、超时、停止等待三处都会显示这次到底走的哪条，不用再靠猜。

## V7.2 · 2026-08-11

**修复**
- **进度条不再编造进度。** 以前显示的百分比是 `max(服务端进度, 按经过时间估算的曲线)`
  —— 当初嫌「一直卡在 0%」难看，就凭时间编了一条。代价是：服务端把任务挂起、
  progress 从头到尾是 0 时，界面照样一路爬到 17%，让人以为在跑，一等 20 分钟，
  最后什么都没有。**编出来的进度条比空进度条有害得多。**
  现在服务端不报进度就显示「服务端未报告进度」+ 滚动条纹（表示还在查询），
  绝不谎称完成了百分之多少。
- **卡住提示不再一闪一闪。** 提示以前是渲染完再追加的，而每 5 秒一次的轮询会重写整块，
  于是状态在 queued / running 之间跳变时提示反复出现又消失。现在提示是渲染的一部分，
  一旦出现就不会再消失。

**改进**
- 长时间零进度改为分级提示：3 分钟给出可能原因和「停止等待」；
  **8 分钟仍然零进度，状态直接标为「⚠️ 疑似卡住」**，并明确说明这几乎可以确定不是慢、
  而是卡住了，最常见原因是参考图没通过内容审核 —— 服务端有时不返回拒绝，
  只是把任务无限期挂着，继续等大概率仍是一场空。
  （标的是「疑似卡住」而不是「失败」：任务并没有被服务端确认失败，措辞不替它下结论。）

## V7.1 · 2026-08-11

**修复**
- **参考图被内容审核拒绝时，提示会把你带偏。** 服务端把原因放在 `code`
  （`content_policy_violation`）里，人话放在 `message` 里，而应用只读了 message，
  于是「内容被拒」被归类成「参数可能不被支持，请更换画幅/时长」—— 完全指错方向。
  现在会一并读 `code`，并直接说清：**首要嫌疑是参考图**，跟画幅/时长/清晰度无关，改参数没用。
- **任务被服务端挂起时，不再让你干等 20 分钟一场空。** 有时服务端既不拒绝也不开始，
  就一直挂着。现在排队满 3 分钟仍未开始处理，会明确告诉你两种可能（后端排队 /
  卡在内容审核），并给一个「停止等待」按钮；停止后会留下 `video_id`，
  万一任务后来跑完了还能凭它查回来。20 分钟超时那屏也一并加上说明和 ID。
- 轮询阶段失败以前只甩一段原始 JSON。现在和提交阶段一样给出人话提示，
  并说明参考图已保留。

## V7.0 · 2026-08-10

**修复**
- 切换到 English 后，视频参数下面那行结果（尺寸 / 时长 / 帧数 / 帧率）
  和视频画廊的空态文案仍是中文。这两处是 JS 拼出来的，不带翻译标记，
  切语言时扫描不到；现在会跟着一起重算。

## V6.9 · 2026-08-10

> V6.6–V6.8 是过程版本，没有单独发布，改动都并在这一条里。

**修复**
- 图生视频生成一次之后再点「生成视频」，会提示「至少需要上传首帧图片」。
  参考图在提交时被清空了、成功后也没恢复，从画廊选来的那张就此丢失。现在不再清空。
- **选了画幅却不作数已修**。出片比例其实由参考图决定 —— 竖图配 16:9，出来仍是竖版。
  现在提交前会先把参考图放进你选的画幅里再送上去，画幅选什么就是什么。
- **参考图不再被裁掉**。比例差得多时（例如 3:4 的图配 16:9），完整原图等比居中，
  两侧用同一张图的模糊放大版补足 —— 不裁、不拉伸、也不是生硬的黑边。
- **画幅选对了却有细模糊边已修**。档位尺寸要对齐到 64，并不精确等于名义比例
  （720p 的 3:4 实际是 832×1088），这 2% 的差此前也去补幅，于是两侧各留下几像素。
  现在比例足够接近时直接填满。
- **1080p 选 10 秒会失败已修**（`num_frames exceeds max frames for the resolved
  resolution and ratio`）。除了文档写明的 441 帧，服务端还有一条按分辨率的上限：
  同样 297 帧，720p 能过、1080p 被拒。现在各档的真实时长上限直接标在时长下面；
  万一还是被拒，会记住真实上限并自动缩短重投，不再把报错甩给你。
- 出错时展示的「原始响应」曾经是上一次成功的响应，排查时会被带偏；现在是本次的。

**改进**
- 时长换算改为**向上取整**。`num_frames` 必须是 `8n+1`，所以出片只会比你选的长
  一点点，不会再短。默认帧率改为 30。
- 界面上的「校准」字样全部去掉。实测帧率改为静默进行，并且按你请求的帧率分档记录，
  换帧率不会串味 —— 你不需要知道它的存在。
- 选好参考图后，画幅下面会提前说明会发生什么（例如「参考图是 3:4：会完整保留，
  两侧用模糊背景补足；改选 3:4 可占满画面」），不用等出片了才发现。
- 视频参数区重排：时长 / 画幅 / 清晰度 / 帧率 四个等宽同排，下面用一行说清结果 ——
  `1280×704 · 10.2 秒 · 305 帧 · 30 fps · 最长 14.2 秒`，
  只在撞上限或参考图比例不符时才多一句提示。

## V6.5 · 2026-08-09

**修复**
- 视频卡片的「提示词 → 重新编辑」不再跳到图片生成页。此前它和图片共用同一个弹窗，
  而弹窗里写死了图片输入框，导致视频提示词被填进了图片提示词框。
- **视频时长偏短已修**。根因不是丢帧，是帧率不符：服务端固定按 30fps 出片，
  而应用按 24fps 换算帧数，于是 10 秒档实际只有 8 秒。现在应用会在你第一次
  生成后**自动实测服务端的真实帧率**，之后按实测值换算，选 10 秒就是 10 秒。

- **视频分辨率对不上已修**。服务端只输出固定档位（480p / 720p / 1080p ×
  16:9 / 9:16 / 1:1 / 4:3 / 3:4），此前应用送的 1152×768 之类不在档位上，
  会被就近映射，所以你选竖版却拿到 832×1088。现在画幅改为「宽高比 + 清晰度」
  两个选项，直接按服务端档位提交，选什么就是什么。
- 补上 `num_frames ≤ 441` 的上限校验。此前自定义长时长会送出超限的帧数导致请求失败。

**新增**
- 视频时长支持**自定义秒数**，不再只有 3/5/10/15 四档。输入框下方实时显示
  将要请求的帧数、预计时长，以及触及 441 帧上限时的提示。
- 画幅新增 4:3 与 3:4，并显示将要请求的确切像素尺寸。
- 视频生成完成后，同时显示**服务端返回的** `size` / `seconds` 与**播放器实测的**
  分辨率、时长、帧率；两者与你的请求不一致时，直接引用服务端的
  `size_mapping.message` 说明原因。

## V6.4 · 2026-08-01

**新增**
- **发布形态改为 zip**：Releases 现在提供 `UseNowAIStudio-x.y.z-win64.zip`（内含主程序、中英文使用说明、SHA256 校验值），不再直接挂裸 exe。解压后双击即用，首次打开基本不会再被 Windows SmartScreen 拦下。
- **Agnes AI 国内直连**：默认接口地址改为 `https://apihub.agnes-ai.cn/v1`，国内用户无需代理即可使用。
- API 设置与新手引导里新增「线路」一键切换：**国内直连 / 国际站**，随时互换。同一账号、同一 Key，两站通用。
- 使用指南同步更新注册地址：国内 `agnes-ai.cn`，海外 `agnes-ai.com`。

> 已保存过旧地址的老用户不受影响；想切到国内线路，打开 ⚙️ API 设置点一下「国内直连」即可。

## V6.3 · 2026-07-28

**修复**
- 英文界面补齐：底部「开发者 / 许可协议 / 开源许可」三处此前仍是中文，现已随界面语言切换。
- 修复「导出全部数据」在素材较多时必定失败（`Invalid string length`）的问题。备份改为 ZIP 打包，图片 / 视频以二进制单独入包，**不再有体积上限**，同时体积约省 25%，打包与恢复都有进度显示。
- 导入兼容：新版 `.zip` 与旧版 `.json` 备份都能正常恢复。
- 导出 / 导入失败时补上中文原因说明（体积超限 / 磁盘空间不足 / 内存不足）。

## V6.2 · 2026-07-27

**新增**
- **内置 Claude 官方 API 适配**：直接填 `https://api.anthropic.com/v1` + `sk-ant-` 开头的官方 Key 即可使用，对话、流式输出、思考过程、识图、连接测试全部可用。
- Claude 预设模型更新为 Claude Opus 5 / Sonnet 5 / Haiku 4.5。

## V6.1 · 2026-07-24

- 提示词字数统计改为纯计数，不再显示会引起误会的「上限」。

## V6.0 · 2026-07-24

**重新生成不再覆盖旧答案**（本次重点）
- 点「重新生成」后，新旧答案都保留，底部出现 `‹ 1/2 ›` 切换器随时对比，文字与图片都支持。
- 每个版本各自记住自己用的模型名。
- 修复：切换器要重新进入对话才出现、切回上一版后切换器消失、重新生成会把整份附件正文重新贴出来等问题。
- 修复：一边等生成、一边切到别的对话时，结果会跑错对话的问题。

## V5.x · 2026-07-24

- 历史记录：加入**项目下拉筛选**与收藏，保存条数大幅提高。
- 技能 Skill：「手动添加」移到「导入 Skill」旁边；**我的技能**与**快捷启用技能**都支持搜索。
- 修复：不在对话页时点右上角「历史记录」没反应。
- 视频生成进度不再长时间停在 0%。
- 重新进入对话时，之前上传的图片能正常显示。
- 导出文件时只显示附件名，不再把整份文件正文贴进去。

## V4.x · 2026-07-23

- 全新 **使用指南**页：三步上手 + 分区折叠，信息不再堆在一起。
- 图片提示词库新增 7 套人像大片预设（职业肖像、时尚写真、街拍、漫画化等）。
- 修复：长提示词弹窗显示不全且无法滚动。
- 所有失败提示补上中文解释，并尽量说明服务器返回的**具体**原因。
- 打包版禁用开发者工具与右键菜单。
- 新增正式《最终用户许可协议（EULA）》与首次运行同意流程、第三方开源许可声明页。
- 更换全新产品图标 / Logo。

## V3.x · 2026-07-22 ~ 07-23

- 修复图生视频只输出一张定格画面（黑屏）的问题。
- 视频历史记录容量提升。
- 首个 Windows 免安装单文件版本发布。

---

<sub>更早的版本为内部开发版本，未公开发布。</sub>
